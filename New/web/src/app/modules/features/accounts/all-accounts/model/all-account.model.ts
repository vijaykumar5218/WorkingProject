import {SubHeaderTab} from 'shared-lib/models/tab-sub-header.model';
export interface AllAccountPageText {
  tabs: Array<SubHeaderTab>;
  nonMxNetworthPlaceholderText: string;
  nonMxSpendingPlaceholderText: string;
  nonMxBudgetPlaceholderText: string;
}
